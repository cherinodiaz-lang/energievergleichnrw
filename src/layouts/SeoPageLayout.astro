---
import "@/styles/global.css";
import MicrosoftClarity from "@/components/MicrosoftClarity.astro";
import { SEO_CONFIG } from "@/lib/seo-config";

const GOOGLE_FONTS_URL =
  "https://fonts.gstatic.com";

interface SchemaValue {
  [key: string]: unknown;
}

interface Props {
  title: string;
  description: string;
  canonicalPath?: string;
  keywords?: string;
  ogTitle?: string;
  ogDescription?: string;
  ogImage?: string;
  ogType?: string;
  twitterCard?: string;
  twitterTitle?: string;
  twitterDescription?: string;
  twitterImage?: string;
  noindex?: boolean;
  schemas?: SchemaValue[];
}

const {
  title,
  description,
  canonicalPath = Astro.url.pathname,
  keywords,
  ogTitle = title,
  ogDescription = description,
  ogImage = SEO_CONFIG.defaultOgImage,
  ogType = "website",
  twitterCard = "summary_large_image",
  twitterTitle = ogTitle,
  twitterDescription = ogDescription,
  twitterImage = ogImage,
  noindex = false,
  schemas = [],
} = Astro.props;

const canonicalUrl = new URL(canonicalPath, SEO_CONFIG.siteUrl);
canonicalUrl.hash = "";
canonicalUrl.search = "";

if (canonicalUrl.pathname !== "/") {
  canonicalUrl.pathname = canonicalUrl.pathname.replace(/\/+$/, "");
}

const robots = noindex
  ? "noindex, nofollow"
  : "index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1";
---

<!doctype html>
<html lang="de" class="w-full h-full">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="alternate icon" href="/favicon.ico" />
    {keywords && <meta name="keywords" content={keywords} />}
    <meta name="robots" content={robots} />
    <meta name="author" content="energievergleich.shop" />
    <link rel="canonical" href={canonicalUrl.toString()} />
    <link rel="alternate" hreflang="de-DE" href={canonicalUrl.toString()} />
    <link rel="alternate" hreflang="de" href={canonicalUrl.toString()} />
    <link rel="alternate" hreflang="x-default" href={canonicalUrl.toString()} />

    <!-- Preconnect & preload for Core Web Vitals (LCP) -->
    <link rel="preconnect" href={GOOGLE_FONTS_URL} crossorigin="" />
    <link rel="dns-prefetch" href="//fonts.googleapis.com" />
    <!-- Hero image served from wixstatic.com — preconnect reduces LCP -->
    <link rel="preconnect" href="https://static.wixstatic.com" crossorigin="" />
    <link rel="dns-prefetch" href="//static.wixstatic.com" />
    <link rel="dns-prefetch" href="//www.googletagmanager.com" />
    <link rel="preconnect" href="https://www.googletagmanager.com" crossorigin="" />
    <!-- Verivox calculator partner domain -->
    <link rel="preconnect" href="https://partner.vxcp.de" crossorigin="" />

    <!-- Preconnect Clarity -->
    {SEO_CONFIG.clarityProjectId && (
      <link rel="preconnect" href="https://www.clarity.ms" crossorigin="" />
    )}
    <link rel="dns-prefetch" href="//www.clarity.ms" />

    <!-- GA4 Consent Mode V2 — MUST be first script, before any other JS (GDPR) -->

    <meta property="og:title" content={ogTitle} />
    <meta property="og:description" content={ogDescription} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:url" content={canonicalUrl.toString()} />
    <meta property="og:type" content={ogType} />
    <meta property="og:site_name" content={SEO_CONFIG.siteName} />
    <meta property="og:locale" content="de_DE" />

    <meta name="twitter:card" content={twitterCard} />
    <meta name="twitter:title" content={twitterTitle} />
    <meta name="twitter:description" content={twitterDescription} />
    <meta name="twitter:image" content={twitterImage} />

    {SEO_CONFIG.googleSearchConsoleVerification && (
      <meta
        name="google-site-verification"
        content={SEO_CONFIG.googleSearchConsoleVerification}
      />
    )}

    {schemas.map((schema, index) => (
      <script
        is:inline
        type="application/ld+json"
        set:html={JSON.stringify(schema)}
        data-schema-index={index}
      />
    ))}
    <slot name="head" />
  </head>
  <body class="w-full h-full">
    <MicrosoftClarity />
    <slot />
  </body>
</html>
